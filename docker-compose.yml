services:
  db:
    image: timescale/timescaledb:latest-pg17
    environment:
      POSTGRES_USER: wqm
      POSTGRES_PASSWORD: wqm_dev
      POSTGRES_DB: water_quality
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
